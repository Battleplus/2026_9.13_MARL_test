# 2026-09-14 进度

## 本机最小复现重跑

- 使用公开上游 `catezi/MAPT` submodule，固定 commit `0b4ef2712995681febca7631f9a27e1b0dccccbf`。
- 执行命令：`python scripts/run_mapt_smoke.py --count 64 --device cpu --report results/first_round/windows_fallback_smoke_rerun_2026-09-14.json`。
- 环境：Windows Python 3.14.4，PyTorch 2.13.0+cpu；`torch.cuda.is_available()` 为 `False`。
- 结果：fixture loader 通过，train/val 51/13；obs `float32`；action `int64`；label、mask、trajectory length 和 batch shape 通过。
- Reward model：2 个 epoch，loss `2.5770144975 -> 0.8373570889`；eval loss `0.1857349557`；checkpoint 保存/重新加载通过。
- 策略侧 learned reward inference：通过，输出形状 `(1, 3, 1)`。

## 结果边界

以上是独立合成数据上的 Windows fallback 最小闭环，不是论文官方偏好数据，也不是 SMAC `3m`
正式长训练结果；没有据此声称论文回报或胜率已复现。正式复现仍需 Linux/Python 3.8/PyTorch
1.10.2、SC2 4.10、SMAC v0.1-beta1、PySC2 和可验证的数据来源。
