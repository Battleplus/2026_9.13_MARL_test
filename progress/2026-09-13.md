# 2026-09-13 进度

## 已完成

- 检查本地 4 篇论文 PDF 的主题、实验和代码声明。
- 确认 MAPT 是本批论文中唯一直接面向 MARL 的方法论文。
- 核对 MAPT 官方公开仓库并做静态审计。
- 记录 MAPT 当前审计提交 `0b4ef2712995681febca7631f9a27e1b0dccccbf`。
- 确认 MAPT 官方数据链接当前已失效。
- 确认 MAPT 仓库缺少 LICENSE、测试和 CI，且存在旧依赖、文件名错误、绝对路径和脚本覆盖不全等问题。
- 确认 Christiano et al. 2017 有 MIT 许可的公开实现，但不属于 MARL。
- 制定 MAPT 的分阶段复现目标、双轨数据策略、验收标准和停止规则。
- 新建本仓库的状态、文档、进度、实验和结果结构。
- 编写可直接交给另一个 AI 的执行话术。

## 本机环境观察

- Windows 11 Home 64-bit。
- 约 16 GB 内存，16 个逻辑处理器。
- 未检测到 `nvidia-smi`。
- 结论：本机可做文档和静态审计，不建议直接承担论文级全量训练。

## 决策

- 主复现任务：MAPT。
- 第一基准：SMAC `3m`。
- 核心困难任务：SMAC `3s5z`。
- 原始数据不可得时采用合成教师独立重建，并明确标记数据来源。
- 完整训练默认使用 Linux + NVIDIA GPU。

## 下一步

## 第一轮执行记录

- 远端 `main` 起点只有审计文档骨架；已把上游 `catezi/MAPT` 固定为 submodule commit `0b4ef2712995681febca7631f9a27e1b0dccccbf`。
- 新增 `env/mapt-linux-lock.yml`：目标为 Python 3.8.18、PyTorch 1.10.2、torchvision 0.11.3、CUDA 11.3；新增历史依赖与 Python 3.12 smoke 依赖说明。
- 新增 `scripts/install_sc2.sh`，不再假设 `$EXP_DIR/pymarl`；新增 reward/policy wrapper，避免作者绝对路径并修正 `--log_interval`。
- `patches/mapt/0001-reproducibility.patch` 已用 `git -C upstream/MAPT apply --check` 验证；只提交已验证的依赖改名/占位依赖移除与 SMAC policy import path 修复，不修改 submodule 内容。
- 反推出 pickle schema：顶层 list；`traj0`/`traj1` 各含 `obs`、`actions`；另有标量 `label`；固定长度 loader 生成 timesteps，策略 inference 生成 `(B,T,N)` mask。
- 运行独立合成 fixture：64 对，`obs=float32`、`actions=int64`、`T=4`、`N=3`、`obs_dim=5`、离散 action dim=9；loader train/val 为 51/13；`label` 编码覆盖 one-hot 与 tie soft label。
- Windows fallback smoke 真实通过：Python 3.14.4 + PyTorch 2.13.0+cpu；两次训练 loss `2.5770144975 -> 0.8373570889`，eval loss `0.1857349557`，checkpoint save/reload 通过，策略形状 `(1,3,1)`。
- Linux WSL 探测：Ubuntu 24.04.4、Python 3.12.3、RTX 3060 Laptop 6 GiB；无 torch/SC2/SMAC/PySC2。PyTorch Linux wheel 下载至约 304 MB 后读取超时，Linux smoke 明确标为待执行。

## 当前 blocker 与长训练资源门

- 官方偏好数据链接仍失效；当前 fixture 是 independent reproduction，不能用于声称论文数值复现。
- 进入 SMAC `3m` 长训练前，需要可复现 Linux Python 3.8 环境、PyTorch 1.10.2/CUDA 11.3、SC2 4.10、SMAC v0.1-beta1、PySC2 3.0.0，并先通过 Linux smoke。
- 建议单 GPU 至少 16 GB 显存、32 GB 系统内存和 50–100 GB 磁盘；论文使用单张 V100。当前 RTX 3060 6 GB/约 16 GB RAM 只适合 fallback 和调试，不承诺论文级长训练。
- 尚未启动任何长时间训练；尚未生成 50,000 对数据。
